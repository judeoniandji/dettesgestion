{% extends 'base.html.twig' %}

{% block title %}Gestion dette_boutique{% endblock %}

{% block body %}
    <header class="bg-cyan-800 text-white p-4">
        <h1 class="text-center text-xl font-bold">Clients</h1>
    </header>

    <div class="flex min-h-screen">

        <!-- Sidebar -->
        <aside class="bg-gray-200 w-1/6 p-4">
            <nav>
                <ul class="space-y-2">
                    <li><a href="#" class="block p-2 bg-cyan-600 text-white rounded">Accueil</a></li>
                    <li><a href="#" class="block p-2 bg-gray-500 text-white rounded">Menu 2</a></li>
                    <li><a href="#" class="block p-2 bg-gray-500 text-white rounded">Menu 3</a></li>
                </ul>
            </nav>
        </aside>

        <main class="flex-1 p-4">
            {# Affichage des erreurs de formulaire #}
            {{ form_start(formClient) }}
                <div class="mb-4">
                    {{ form_errors(formClient) }} {# Affiche les erreurs du formulaire #}
                </div>
                {{ form_widget(formClient) }} {# Rend tous les champs du formulaire #}
                <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded-md">Enregistrer</button>
            {{ form_end(formClient) }} {# Terminer le formulaire #}
        </main>
    </div>
{% endblock %}
